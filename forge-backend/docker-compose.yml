services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: forge-backend
    image: forge-backend:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
